var React = require('react');
var WarpWrapper = require('./WarpWrapper');

var App = React.createClass({
  render: function() {
    return (
      <WarpWrapper />
    );
  }
})

module.exports = App;
